# Threads 投稿文案（フォーマット準拠）

`threads/投稿文案フォーマット.md` の**朝・昼・夜いずれか一方**に沿った **Threads 下書き Markdown** を生成する。文体・トーンは **`.cursor/skills/threads-marusho-posts/SKILL.md`**、手順・出力構造は **`.cursor/skills/threads-post-from-format/SKILL.md`** が正本。

## スロット（第1引数・必須）

ユーザーは、コマンド参照の**同じメッセージ内**で、次のいずれかを**明示**する（先頭でも文中でも可。エージェントは会話から最初に現れたスロットを採用する）。

| 指定（日本語） | 英語別名 | 生成する時間帯 |
|----------------|----------|----------------|
| **朝** | `morning`, `am` | 朝（予告・前日の振り返り） |
| **昼** | `midday`, `noon` | 昼（調査直後・メモ口調） |
| **夜** | `evening`, `night` | 夜（note 公開後） |

**例**

- `@threads-post-from-format.md 朝` ＋ `@note/.../記事.md`
- `@threads-post-from-format 昼` と、続けてブリーフや URL を貼る
- `@threads-post-from-format.md` のメッセージ本文に「**夜**でお願い」と書く

スロットが**複数**ある場合はユーザーにどれで出すか確認する。スロットが**どれにも当たらない**場合は確認してから進める。

## エージェントが最初にすること（必須）

1. **`.cursor/skills/threads-post-from-format/SKILL.md` を読む**（スロット解釈・出力テンプレ・保存規則）。
2. **`.cursor/skills/threads-marusho-posts/SKILL.md` を読む**（まるしょー文体・チェックリスト）。
3. **`threads/投稿文案フォーマット.md` を読む**（埋め字・見出し・ファイル名規則の参照）。

## 入力の取り方

| パターン | 扱い |
|----------|------|
| **`@` で note 底稿・ブリーフ・速報ログ** | その `.md` を読み、主トピック・一次リンク・日付を拾う |
| **会話内の要点のみ** | スキルの「情報不足時」に従い、確定できない項目はプレースホルダまたは短い確認 |
| **保存先・ファイル名** | ユーザー指定が最優先。未指定ならスキルの既定（`threads/YYYY-MM-DD-...`） |

## 出力（ファイル）

| 項目 | 内容 |
|------|------|
| **既定の保存先** | `threads/` |
| **ファイル名** | `YYYY-MM-DD-〈スラッグ〉-threads-morning.md` など（スロットに応じ `-morning` / `-midday` / `-evening`）。スキル本文の表に従う。 |
| **本文構造** | `## 投稿1（メイン）` / `## 投稿2` / `## 1本にまとめる場合（参考）` を含む完全な下書き（フォーマットどおり）。 |
| **昼の文体** | 報道が根拠のときは「～と報じられています」「～とも書かれています」等で媒体に帰属（`threads/2026-04-14-anthropic-mythos-pentagon-threads-midday.md` と同型）。投稿1の末尾は固定で「夜に、一次リンクと用語の短い注を含めて note にまとめる予定です。」（`続く↓` の直前）。 |
| **Threads 向け本文** | 貼り付け用の段落に Markdown 太字 `**` を入れない（Threads では反映されない）。 |

## チャットでの返答

- 採用したスロット（朝／昼／夜）
- **保存したファイルのパス**（相対パスで明示）
- プレースホルダが残っている場合は、差し替え箇所を箇条書き

## ツール

- **Read** でスキル3本＋入力 `@` ファイル＋必要なら `threads/` の既存例を参照。
- **Write** で `threads/` に新規ファイルを保存（ユーザーが上書きパスを指定した場合はそのパス）。

## 関連

- フォーマット: `threads/投稿文案フォーマット.md`
- スキル（手順）: `.cursor/skills/threads-post-from-format/SKILL.md`
- スキル（文体）: `.cursor/skills/threads-marusho-posts/SKILL.md`
